# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
*.egg-info

# Virtual environments
.venv

# Deps are pinned in requirements.txt, not uv's own lockfile workflow —
# this regenerates as an inert byproduct of some uv commands
uv.lock

# Secrets
.env

# Downloaded/scraped data and embedding caches
data/*
!data/.gitkeep
